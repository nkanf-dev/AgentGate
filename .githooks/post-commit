#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
if ! "$repo_root/scripts/archive-skmb.sh"; then
  printf 'skmb: post-commit archive failed; commit was already created\n' >&2
fi
